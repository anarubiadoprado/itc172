@model IEnumerable<FinalProject1.Models.GrantReview>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GrantReviewDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GrantRequestStatus)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GrantAllocationAmount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Employee.EmployeeKey)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GrantRequest.GrantRequestExplanation)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GrantReviewDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GrantRequestStatus)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GrantAllocationAmount)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Employee.EmployeeKey)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GrantRequest.GrantRequestExplanation)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GrantReviewKey }) |
            @Html.ActionLink("Details", "Details", new { id=item.GrantReviewKey }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GrantReviewKey })
        </td>
    </tr>
}

</table>
